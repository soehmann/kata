/**
 * WordSlotmachine - Testsuite ( using QUnit )
 */

test( "test the test (Examples)", function() {

	ok( true, "true succeeds" );
	equal( 0, 0, "Zero; equal succeeds" );
	equal( '', 0, "Zero; equal succeeds" );

	strictEqual( 0, 0, "Comparision with strict" );

	deepEqual( 0, 0, 'deepequel' );
});

